# Copy to secrets.stage.local.ps1 (gitignored), set password, then:
#   ./scripts/deploy.ps1 -Environment stage

$sqlAdminPassword = 'ChangeMe-Stage-Sql-Password1!'

# Optional: override auto-detected public IP for SQL firewall. Use '' to skip client IP rule.
# $clientIpAddress = '203.0.113.10'
