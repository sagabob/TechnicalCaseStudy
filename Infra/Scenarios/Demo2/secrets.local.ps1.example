# Copy to secrets.local.ps1 (gitignored), set password, then run:
#   ./scripts/deploy.ps1              # dev (default)
#   ./scripts/deploy.ps1 -Environment stage   # or use secrets.stage.local.ps1.example

$sqlAdminPassword = 'ChangeMe-Sql-Password1!'

# Optional: override auto-detected public IP for SQL firewall. Use '' to skip client IP rule.
# $clientIpAddress = '203.0.113.10'
